# 周记

**本周学习主题：ATOM 分子动力学预测模型及分子结构表征方法学习**

## 一、本周主要内容

1. 清楚模型的输入输出

2. 数据集和源码

3. 模型如何从原子坐标中获得分子空间结构？

4. 为什么需要 Radius Graph 和 RWPE？

5. 通过了辐射安全与防护考核。

---

## 二、传统分子动力学与神经网络预测的区别

首先学习了传统分子动力学（Molecular Dynamics，MD）的基本过程。

传统 MD 根据分子的当前状态计算原子所受到的力，再利用牛顿运动方程：

不断更新原子的位置和速度，从而得到完整的分子运动轨迹。

其基本过程可以表示为：

```text
当前分子状态
    ↓
计算势能/原子受力
    ↓
牛顿运动方程
    ↓
数值积分
    ↓
下一个时间步
    ↓
重复计算
    ↓
得到完整分子轨迹
```

因此，传统 MD 本质上是一个逐时间步进行的物理模拟过程。

**直说缺点**：计算量大O（n3)，然后前一个时间点来预测下一步（串行迭代，失真，不利于长远时间预测）

两者概括：

```text
传统 MD：

当前状态
   ↓
物理规律
   ↓
下一时刻
   ↓
继续迭代


ATOM：

当前状态 + 未来时间
   ↓
神经网络
   ↓
未来状态
```

---

## 三、ATOM 的输入数据

进一步学习后，我认识到 ATOM 并不是直接输入“苯环”“羟基”等人类定义的分子结构标签，而是从原子级信息开始建立分子表示。

ATOM 的基本输入可以理解为：

表示 个原子的三维坐标；

表示原子的速度；

表示原子类型；

同时输入未来需要查询的时间：

最终模型需要预测：

即 个未来时间点上 个原子的三维坐标。

# 四、数据集与源码

数据集：https://ses.library.usyd.edu.au/handle/2123/35120

包含：

- 一个分子；
- 对该分子进行 MD 模拟；
- 得到多个时间步的原子状态。

源码：https://github.com/luke-a-thompson/ATOM

---

# 五、模型如何获得分子的空间结构

这是本周学习过程中重点思考的问题。

例如，对于一个苯环：

```text
        C1
      /    \
    C2      C6
    |        |
    C3      C5
      \    /
        C4
```

模型并不会直接得到：

```text
这是一个苯环
```

而是首先得到：

```text
6个C原子的类型
+
6个C原子的三维坐标
```

因此需要从坐标中进一步构建原子之间的空间关系。

---

## 4.1 Radius Graph：建立空间邻居关系

ATOM 根据原子之间的欧氏距离建立 Radius Graph：

其中论文中使用了固定的距离阈值。

因此：

```text
原子坐标 X
    ↓
计算任意两个原子的距离
    ↓
距离小于 ε？
   ↙       ↘
 是         否
 ↓           ↓
建立边       不建立边
    ↓
Radius Graph
```

这一步解决的是：

> **“哪些原子在当前三维空间中彼此接近？”**

需要注意，这里的 Radius Graph 是**空间邻居图**，并不严格等同于化学键图。

因此，我之前周记中“根据靠得近假设有键”的表述需要进一步修改。

更准确的说法应该是：

> 根据原子之间的空间距离建立邻居关系，而不是直接判断化学键。

这样可以避免把“空间邻居”和“化学键”混为一谈。

---

# 六、RWPE：进一步描述原子在空间图中的结构位置

建立 Radius Graph 后，每个原子不仅有自己的坐标，还可以知道：

> “我和哪些原子相邻？”

但是，仅仅知道直接邻居还不够。

例如：

```text
链：

A —— B —— C —— D


环：

A —— B
|     |
D —— C
```

两种结构中的某些原子都可能有两个邻居，但它们更大尺度的图结构并不一样。

因此 ATOM 进一步使用 RWPE（Random Walk Positional Encoding）。

首先根据邻接矩阵 和度矩阵 构造：

然后计算：

并提取对角元素：

可以直观理解为：

> **从某个原子出发，在空间邻居图中随机走若干步后，回到原来的原子的概率。**

因此 RWPE 不是一个需要单独训练的模型，而是一种根据当前空间图计算得到的结构特征。

这一点也解决了我之前产生的一个疑问：

> RWPE 本身并不是“模型学习出来的”，真正需要训练的是后面的神经网络。

---

# 七、模型如何通过这些信息学习“分子内部结构”

这是本周最重要的理解之一。

最开始我容易认为：

> 模型需要先识别出“苯环”，然后才能进行分子运动预测。

但进一步学习后发现，ATOM 并没有设置一个单独的“苯环识别模块”。

它的思路更接近：

```text
原子类型 Z
      +
三维坐标 X
      +
速度 V
      ↓
空间距离
      ↓
Radius Graph
      ↓
RWPE
      ↓
结构特征
      ↓
神经网络
      ↓
预测未来运动
      ↓
计算 Loss
      ↓
反向传播
      ↓
更新模型参数
```

因此模型学习的并不是：

而是：

换句话说：

> **模型通过预测未来运动的任务，被迫学习那些对未来运动有帮助的分子结构特征。**

例如，如果某种空间结构会导致某些原子产生协同运动，那么模型为了降低预测误差，就会逐渐学习这种结构与运动之间的关系。

因此：

这比简单地说“模型识别出了苯环”更加准确。

---

# 八、E(3) Equivariance：保证三维空间中的物理一致性

在学习分子结构后，又进一步学习了为什么模型需要考虑三维空间中的旋转和平移。

例如同一个分子：

```text
原始坐标

    ●
   / \
  ●   ●


旋转后

      ●
     / \
    ●   ●
```

虽然坐标发生了变化，但分子本身的物理状态并没有因为我们旋转了坐标系而改变。

因此模型需要满足相应的物理对称性。

ATOM通过 E(3) equivariance 等设计处理这种三维空间对称性。

同时，模型对位置和速度进行范数增强：

也就是说，在原来的三维向量之外加入一个范数信息。

需要注意：

> E(3) equivariance 并不是简单地“多增加一维数据”，而是一种对模型如何处理三维空间信息的约束和设计原则。

---

# 九、ATOM 如何训练

最后，将上述各个部分连接起来，可以得到完整训练流程：

```text
                 MD轨迹数据
                     ↓
              选择当前时间 t
                     ↓
              X_t + V_t + Z
                     ↓
              Radius Graph
                     ↓
                   RWPE
                     ↓
             分子结构表示
                     ↓
            Equivariant处理
                     ↓
              Self-Attention
                     ↓
            输入未来时间 Δt
                     ↓
          预测 P 个未来时间点
                     ↓
             X_future预测值
                     ↓
          与真实 MD 轨迹比较
                     ↓
                    Loss
                     ↓
                 反向传播
                     ↓
                更新模型参数
```

ATOM 的训练数据来自分子动力学轨迹，例如 TG80 等数据集。数据中包含分子在不同时间点的运动状态，模型通过大量这样的训练样本学习未来运动规律。

---

# 本周学习总结

通过本周对 ATOM 的学习，我逐渐从单纯理解“模型预测未来分子位置”，深入到了理解模型为什么能够获得分子结构信息。

目前可以将 ATOM 的核心思路总结为：

其中：

- ：告诉模型原子在什么位置；

- ：告诉模型当前的运动状态；

- ：告诉模型原子是什么元素；

- **Radius Graph**：建立空间邻居关系；

- **RWPE**：提供原子在空间图中的结构位置信息；

- **E(3) Equivariance**：保证三维空间变换下的物理一致性；

- **Self-Attention**：让不同原子之间进行信息交互；

- **Loss + 反向传播**：让模型从真实 MD 轨迹中学习结构与动力学之间的关系。

因此，我目前对 ATOM 的理解已经从：

> “一个预测分子未来位置的神经网络”

逐渐转变为：

> **“一个从原子级三维状态中构建空间结构表示，并通过大量 MD 轨迹学习分子结构与动力学关系的神经算子模型。”**

---

# 下周学习计划

下一阶段准备进一步结合 ATOM 论文和代码，重点分析模型内部具体的数据维度和网络结构。

重点学习：

1. 分别如何进入模型；

2. Radius Graph 在代码中如何构建；

3. RWPE 的具体维度和计算过程；

4. Equivariant Lifting 到底做了什么；

5. Self-Attention 的输入和输出分别是什么；

6. 一个原子如何从最初的三维坐标表示变成高维 embedding；

7. 最终如何从高维特征恢复成：
   
   的未来三维坐标；

8. 结合 Dataset、DataLoader、Model、Loss 代码，完整跑通 ATOM 的训练流程。
